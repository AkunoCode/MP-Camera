# Minimal runtime requirements detected from `.venv311` (2025-11-20)
# This file lists packages the application imports directly; transient
# dependencies will be resolved by pip when installing.

# GUI
PyQt6==6.4.2

# Image / array
opencv-python==4.10.0.84
numpy==2.3.4

# Environment / HTTP
python-dotenv==1.2.1
requests==2.32.5

# ML / inference
# Roboflow SDK used by mpcamera.services.roboflow.RoboflowClient
inference-sdk==1.0.2
scikit-learn==1.7.2
torch==2.5.1
torchvision==0.20.1
ultralytics
rfdetr
supervision
pillow

# If you need full developer/test deps, generate a separate requirements-dev.txt
click==8.3.1
