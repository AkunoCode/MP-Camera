## Networking / helpers

## Image utilities

# Color naming / perceptual matching
# Used by mpcamera.utils.color_utils.get_color_name (KMeans + delta-E matching)

## Inference SDK (optional)
# The official SDK (import name `inference_sdk`, pip package `inference-sdk`) currently
# has wheels that target Python <=3.11 / <3.13 for many releases. If you run the
# application in a Python 3.8–3.11 venv you can install the SDK to use the
# Minimal runtime requirements detected from `.venv311` (2025-11-20)
# This file lists packages the application imports directly; transient
# dependencies will be resolved by pip when installing.

# GUI
PyQt6==6.4.2

# Image / array
opencv-python==4.10.0.84
numpy==2.3.4

# Environment / HTTP
python-dotenv==1.2.1
requests==2.32.5

# ML / inference
inference-sdk==0.61.0
scikit-learn==1.7.2
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# If you need full developer/test deps, generate a separate requirements-dev.txt
click==8.3.1
